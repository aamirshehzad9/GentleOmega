# In env/.env (never commit)
HF_TOKEN=REPLACE_WITH_YOUR_HF_TOKEN
OPENAI_BASE_URL=https://router.huggingface.co/v1
GEN_MODEL=deepseek-ai/DeepSeek-V3.2-Exp:novita
# embeddings: choose one approach below
# Option A (local embeddings):
EMBEDDINGS_BACKEND=local
EMBEDDINGS_MODEL=sentence-transformers/all-MiniLM-L6-v2
# Option B (remote – only if your provider exposes an OpenAI-compatible embeddings model):
# EMBEDDINGS_BACKEND=remote
# EMBEDDINGS_MODEL=text-embedding-3-large

PG_HOST=localhost
PG_PORT=5432
PG_DB=metacity
PG_USER=postgres
PG_PASSWORD=YOUR_PASSWORD
SERVICE_PORT=8000

# GentleΩ Phase 4: EVM Blockchain Integration
CHAIN_RPC=https://your-chain-endpoint
WALLET_PRIVATE_KEY=0x<64-hex>
WALLET_ADDRESS=0x<your-checksummed-address>